# Ralph Progress Log
Started: Sat Jan 31 2026
---

## Codebase Patterns
- Migrations use TypeORM MigrationInterface pattern with `up()` and `down()` methods
- Migration files follow naming: `{timestamp}-{PascalCaseDescription}.ts`
- Use `CREATE TABLE IF NOT EXISTS` pattern for idempotent migrations
- PostgreSQL UUID columns use `gen_random_uuid()` as default
- Timestamps use `TIMESTAMP DEFAULT CURRENT_TIMESTAMP`
- Create indexes separately after table creation
- Use `IF NOT EXISTS` and `IF EXISTS` clauses for idempotent operations
- Build verification: Use `pnpm run build` to verify TypeScript compilation (no separate typecheck script)

## Sat Jan 31 2026 - US-001
- Created audio_files database table migration
- Files changed:
  - `src/database/migrations/1769908667952-CreateAudioFilesTable.ts` (new)
- **Learnings for future iterations:**
  - Migration pattern: Class name follows `{Description}{Timestamp}` format
  - PostgreSQL CHECK constraints used for enum-like values: `CHECK (source_type IN ('article', 'transcription'))`
  - Unique constraints created via `CONSTRAINT unique_name UNIQUE (col1, col2)` syntax
  - Indexes created with DESC ordering for created_at: `CREATE INDEX ... ON table(column DESC)`

## Sat Jan 31 2026 - US-002
- Added OpenAI TTS client initialization to AiService
- Files changed:
  - `src/ai/ai.service.ts` (modified)
- **Learnings for future iterations:**
  - AiService uses OnModuleInit pattern - clients initialized in `onModuleInit()` hook
  - Optional clients (like TTS) should log warnings instead of throwing errors when API key is missing
  - OpenAI client initialization uses default base URL when no custom baseURL is needed
  - Client properties follow pattern: `private clientName: OpenAI | null = null`

## Sun Feb 01 2026 - US-003
- Added text-to-speech method to AiService
- Files changed:
  - `src/ai/ai.service.ts` (modified)
- **Learnings for future iterations:**
  - OpenAI TTS API uses `audio.speech.create()` method with model 'tts-1'
  - Voice options: 'alloy', 'echo', 'fable', 'onyx', 'nova', 'shimmer'
  - Response format 'mp3' ensures MP3 output
  - Convert ArrayBuffer to Buffer using `Buffer.from(arrayBuffer)`
  - Handle missing TTS client gracefully by returning null and logging error

## Sun Feb 01 2026 - US-004
- Added uploadAudioFile method to S3Service
- Files changed:
  - `libs/s3/s3.service.ts` (modified)
- **Learnings for future iterations:**
  - Use `PutObjectCommand` from @aws-sdk/client-s3 for direct uploads
  - Upload pattern: `Body: Buffer`, `ContentType: 'audio/mpeg'`
  - S3Service follows consistent error handling pattern with descriptive messages
  - Import pattern: `import { GetObjectCommand, PutObjectCommand, S3Client } from '@aws-sdk/client-s3'`

## Sun Feb 01 2026 - US-005
- Created AudioFilesService for database operations
- Files changed:
  - `src/audio-files/audio-files.service.ts` (new)
- **Learnings for future iterations:**
  - Service pattern: Inject DatabaseService in constructor
  - Database query pattern: Use Promise wrapper with db.prepare() for INSERT, db.get() for single row SELECT
  - Handle duplicate key errors by checking error code '23505' (PostgreSQL unique violation)
  - Return null on duplicate key errors instead of throwing
  - Export interface types alongside service for type safety
  - Interface pattern: Define row interface for database results, then map to domain interface

## Sun Feb 01 2026 - US-006
- Created AudioFilesModule and integrated into AppModule
- Files changed:
  - `src/audio-files/audio-files.module.ts` (new)
  - `src/app.module.ts` (modified)
- **Learnings for future iterations:**
  - Module pattern: Import required modules (DatabaseModule, S3Module), provide service, export service
  - Module imports in AppModule should be alphabetically ordered for consistency
  - Always add import statement at top of file when adding module to imports array
---
