# Ralph Progress Log
Started: Sat Jan 24 2026
---

## Codebase Patterns
- Empty directories are not tracked by git - they'll be created when files are added to them
- Use `pnpm run build` for typechecking (no separate typecheck script)
- Directory structure follows NestJS conventions with subdirectories for different concerns

## 2026-01-24 - US-001
- **What was implemented**: Created libs/auth directory structure with subdirectories for decorators, guards, strategies, dto, and interfaces
- **Files changed**: 
  - Created libs/auth/ directory structure (empty directories)
- **Learnings for future iterations:**
  - Empty directories aren't tracked by git, but that's fine - they'll exist once files are added
  - Build passes with empty directory structure
  - Following the same pattern as libs/s3 and libs/email
---

## 2026-01-24 - US-002
- **What was implemented**: Created UserLookupProvider interface to decouple AuthService from UsersService
- **Files changed**: 
  - Created libs/auth/interfaces/user-lookup-provider.interface.ts
- **Learnings for future iterations:**
  - Interface imports User entity from src/users/user.entity.ts using relative path (acceptable since User is a domain entity)
  - Interface matches the exact signatures from UsersService: getUserByEmail(email, includePassword) and getUserById(userId)
  - Both methods return Promise<User | null>
---

## 2026-01-24 - US-003
- **What was implemented**: Moved AuthService to libs/auth/ and refactored to use UserLookupProvider interface instead of UsersService
- **Files changed**: 
  - Moved src/auth/auth.service.ts to libs/auth/auth.service.ts (using git mv)
  - Updated AuthService to use UserLookupProvider interface
  - Temporarily updated imports in auth.controller.ts, auth.module.ts, and jwt.strategy.ts (will be properly refactored in later stories)
- **Learnings for future iterations:**
  - Use `import type` for interfaces used in constructor parameters when isolatedModules is enabled
  - When moving services, temporarily update imports in dependent files to keep build passing (they'll be refactored properly in later stories)
  - Git history is preserved when using `git mv` (verified with git log --follow)
  - LoginResponseDto still imported from src/auth/dto/ temporarily (will be moved in US-006)

## 2026-01-24 - US-004
- **What was implemented**: Moved AuthModule to libs/auth/ and refactored to use forRoot()/forRootAsync() pattern with UserLookupProvider
- **Files changed**: 
  - Moved src/auth/auth.module.ts to libs/auth/auth.module.ts (using git mv)
  - Added forRoot() and forRootAsync() static methods to AuthModule
  - Created temporary wrapper module in src/auth/auth.module.ts (will be properly completed in US-009)
  - Updated AuthService to use injection token USER_LOOKUP_PROVIDER_TOKEN
- **Learnings for future iterations:**
  - Use forRootAsync() with useFactory when provider needs dependencies injected
  - Injection token pattern allows decoupling from concrete implementations
  - Wrapper module pattern needed to bridge libs module with domain-specific dependencies
  - Git history preserved when using git mv

## 2026-01-24 - US-005
- **What was implemented**: Moved guards, decorators, and strategies to libs/auth/
- **Files changed**: 
  - Moved src/auth/guards/jwt-auth.guard.ts to libs/auth/guards/jwt-auth.guard.ts (using git mv)
  - Moved src/auth/decorators/public.decorator.ts to libs/auth/decorators/public.decorator.ts (using git mv)
  - Moved src/auth/strategies/jwt.strategy.ts to libs/auth/strategies/jwt.strategy.ts (using git mv)
  - Updated JwtStrategy to import AuthService from relative path within libs/auth
  - Temporarily updated imports in app.controller.ts, app.module.ts, auth.controller.ts, auth.module.ts, users.controller.ts (will be updated to @libs/auth in US-010)
- **Learnings for future iterations:**
  - Relative imports within libs/auth work correctly (e.g., '../decorators/public.decorator')
  - JwtStrategy now imports AuthService from '../auth.service' (relative within libs/auth)
  - Git history preserved for all moved files

## 2026-01-24 - US-006
- **What was implemented**: Moved DTOs to libs/auth/dto/
- **Files changed**: 
  - Moved src/auth/dto/login.dto.ts to libs/auth/dto/login.dto.ts (using git mv)
  - Moved src/auth/dto/login-response.dto.ts to libs/auth/dto/login-response.dto.ts (using git mv)
  - Updated AuthService to import LoginResponseDto from './dto/login-response.dto' (relative within libs/auth)
  - Updated auth.controller.ts to import DTOs from libs/auth/dto/ (temporarily, will use @libs/auth in US-010)
- **Learnings for future iterations:**
  - DTOs maintain all validation decorators after move
  - Git history preserved for moved DTOs

## 2026-01-24 - US-007
- **What was implemented**: Created barrel export for libs/auth
- **Files changed**: 
  - Created libs/auth/index.ts with exports for public API
- **Learnings for future iterations:**
  - Barrel export follows same pattern as libs/s3 and libs/email
  - Exports only public API (module, service, guard, decorator, DTOs)
  - Does NOT export internal interfaces (UserLookupProvider) or strategies (JwtStrategy)

## 2026-01-24 - US-008
- **What was implemented**: Created UserLookupProvider implementation that wraps UsersService
- **Files changed**: 
  - Created src/auth/providers/user-lookup.provider.ts
- **Learnings for future iterations:**
  - Provider implementation stays in src/auth/ since it's domain-specific (wraps UsersService)
  - Uses @libs/auth import for the interface
  - Properly implements both methods with correct types

## 2026-01-24 - US-009
- **What was implemented**: Updated src/auth/ to contain only controller and wrapper module
- **Files changed**: 
  - Updated src/auth/auth.module.ts to use UserLookupProviderImpl from providers/
  - Updated src/auth/auth.controller.ts to import from @libs/auth barrel export
  - Wrapper module properly configures AuthModule.forRootAsync() with UserLookupProviderImpl
- **Learnings for future iterations:**
  - Controller uses @libs/auth barrel export for clean imports
  - Wrapper module pattern bridges libs infrastructure with domain-specific dependencies
  - JwtStrategy still provided in wrapper module (needs AuthService which is exported from libs)

## 2026-01-24 - US-010
- **What was implemented**: Updated all auth imports to use @libs/auth
- **Files changed**: 
  - Updated src/app.module.ts to import JwtAuthGuard from @libs/auth
  - Updated src/app.controller.ts to import Public from @libs/auth
  - Updated src/users/users.controller.ts to import Public from @libs/auth
  - src/auth/auth.controller.ts already uses @libs/auth (from US-009)
- **Learnings for future iterations:**
  - All public API imports now use @libs/auth barrel export
  - Internal imports (like JwtStrategy) still use relative paths since not exported from barrel
  - No references to src/auth/ for infrastructure (only controller and wrapper module remain)

## 2026-01-24 - US-011
- **What was implemented**: Created basic test structure files for auth module
- **Files changed**: 
  - Created libs/auth/auth.module.spec.ts
  - Created libs/auth/auth.service.spec.ts
  - Created libs/auth/guards/jwt-auth.guard.spec.ts
- **Learnings for future iterations:**
  - Tests follow same patterns as libs/email and libs/s3
  - Use jest-mock-extended for type-safe mocks
  - Mock ExecutionContext properly with getHandler() and getClass() methods
  - All tests pass successfully
---
